#namespace_container
  .lead
    = @namespace.description

  = render :partial => 'shared/middle_bar', :locals => {:active => @project, :namespace => @namespace}

  #project_container
    = render :partial => 'projects/show'

